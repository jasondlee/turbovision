Building TV
===========

1. Install gdm-devel in addition to gcc, make, etc
2. export PERLLIB=/path/to/source/turbovision
3. cd turbovision && ./configure && make

Building the SQIG module
========================
1. Install swig
2. cd swig && ./compile.sh -sdm

Running the demo
================
1. cd swig
2. ./runme.sh
